% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qseaExtra.R
\name{getNRPMTable}
\alias{getNRPMTable}
\title{Get NRPM per window}
\usage{
getNRPMTable(
  qseaSet,
  useGroupMeans = FALSE,
  addMethodSuffix = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{qseaSet}{\code{qseaSet}.
Input object containing window-level signal.
\strong{Default:} none (must be supplied).}

\item{useGroupMeans}{\code{logical(1)}.
If \code{TRUE}, average replicates by the \code{group} column in the sample table and
return group means; if \code{FALSE}, return per-sample NRPM.
\strong{Default:} \code{FALSE}.}

\item{addMethodSuffix}{\code{logical(1)}.
If \code{TRUE}, keep the method suffix in column names (e.g., \code{Sample1_nrpm}
or \code{Group_nrpm_means}); if \code{FALSE}, return bare sample/group names.
\strong{Default:} \code{FALSE}.}

\item{verbose}{\code{logical(1)}.
Print progress/messages.
\strong{Default:} \code{TRUE}.}
}
\value{
A tibble/data.frame with one row per genomic window and columns for each
sample (or group), plus any window metadata included by \code{\link[=getDataTable]{getDataTable()}}.
}
\description{
Convenience wrapper around \code{\link[=getDataTable]{getDataTable()}} to extract \strong{NRPM} values
(normalised reads per million). Returns a window × sample (or group) table
for downstream summaries/plots.
}
\examples{
data(exampleTumourNormal, package = "mesa")

# Per-sample NRPM (first rows/columns)
exampleTumourNormal \%>\%
  getNRPMTable() \%>\%
  dplyr::select(1:6) \%>\%
  head()

# Group means instead of individual samples
exampleTumourNormal \%>\%
  getNRPMTable(useGroupMeans = TRUE) \%>\%
  dplyr::select(1:6) \%>\%
  head()

# Keep the method suffix in column names
exampleTumourNormal \%>\%
  getNRPMTable(addMethodSuffix = TRUE) \%>\%
  names() \%>\%
  head()

}
\seealso{
\code{\link[=getDataTable]{getDataTable()}}, \code{\link[=getCountTable]{getCountTable()}}, \code{\link[=getBetaTable]{getBetaTable()}}

Other table-helpers: 
\code{\link{getBetaTable}()},
\code{\link{getCountTable}()},
\code{\link{getDataTable}()},
\code{\link{getSampleGroups2}()},
\code{\link{makeTransposedTable}()},
\code{\link{removeLibraryFactors}()},
\code{\link{removeNormMethodSuffix}()}
}
\concept{table-helpers}
