{
  "name": "R Bioconductor Dev (codespace_env_implementation)",
  "image": "ghcr.io/cruk-mi/mesa/devcontainer:codespace_env_implementation",
  "workspaceFolder": "/workspaces/${localWorkspaceFolderBasename}",
  "customizations": {
    "vscode": {
      "extensions": [
        "REditorSupport.r",
        "ikuyadeu.r",
        "ms-python.python"
      ]
    }
  },
  "postCreateCommand": "conda run -n shiny_env R -e \"if (!requireNamespace('remotes', quietly=TRUE)) install.packages('remotes', repos='https://cloud.r-project.org'); remotes::install_local('.', upgrade='never')\""
}
